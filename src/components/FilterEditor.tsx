'use client'
interface Props { blur: number; brightness: number; contrast: number; grayscale: number; hueRotate: number; saturate: number; onBlurChange: (v: number) => void; onBrightnessChange: (v: number) => void; onContrastChange: (v: number) => void; onGrayscaleChange: (v: number) => void; onHueRotateChange: (v: number) => void; onSaturateChange: (v: number) => void }
// Feature 568: FilterEditor
export default function FilterEditor({ blur, brightness, contrast, grayscale, hueRotate, saturate, onBlurChange, onBrightnessChange, onContrastChange, onGrayscaleChange, onHueRotateChange, onSaturateChange }: Props) {
  return (<div className="p-3 border-b"><div className="text-xs font-semibold text-gray-500 uppercase mb-2">CSS Filters</div>{[{l:'Blur',v:blur,fn:onBlurChange,min:0,max:20},{l:'Brightness',v:brightness,fn:onBrightnessChange,min:0,max:200},{l:'Contrast',v:contrast,fn:onContrastChange,min:0,max:200},{l:'Grayscale',v:grayscale,fn:onGrayscaleChange,min:0,max:100},{l:'Hue Rotate',v:hueRotate,fn:onHueRotateChange,min:0,max:360},{l:'Saturate',v:saturate,fn:onSaturateChange,min:0,max:200}].map(f => <div key={f.l} className="flex items-center gap-2 mb-1"><span className="text-xs w-16 text-gray-400">{f.l}</span><input type="range" min={f.min} max={f.max} value={f.v} onChange={e => f.fn(Number(e.target.value))} className="flex-1" /><span className="text-xs w-8 text-right">{f.v}</span></div>)}</div>)
}