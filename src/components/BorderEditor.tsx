'use client'
interface Props { width: number; style: string; color: string; radius: number; onWidthChange: (w: number) => void; onStyleChange: (s: string) => void; onColorChange: (c: string) => void; onRadiusChange: (r: number) => void }
// Feature 564: BorderEditor
export default function BorderEditor({ width, style, color, radius, onWidthChange, onStyleChange, onColorChange, onRadiusChange }: Props) {
  return (<div className="p-3 border-b"><div className="text-xs font-semibold text-gray-500 uppercase mb-2">Border</div><div className="grid grid-cols-2 gap-2 text-xs"><div><label className="text-gray-400">Width</label><input type="number" value={width} onChange={e => onWidthChange(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div><div><label className="text-gray-400">Style</label><select value={style} onChange={e => onStyleChange(e.target.value)} className="w-full border rounded px-1 py-0.5"><option>solid</option><option>dashed</option><option>dotted</option><option>double</option><option>none</option></select></div><div><label className="text-gray-400">Color</label><div className="flex gap-1"><input type="color" value={color} onChange={e => onColorChange(e.target.value)} className="w-6 h-6" /><input value={color} onChange={e => onColorChange(e.target.value)} className="flex-1 px-1 py-0.5 border rounded font-mono text-[10px]" /></div></div><div><label className="text-gray-400">Radius</label><input type="number" value={radius} onChange={e => onRadiusChange(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div></div></div>)
}