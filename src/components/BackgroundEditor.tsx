'use client'
interface Props { type: string; color: string; imageUrl: string; size: string; repeat: string; position: string; onTypeChange: (t: string) => void; onColorChange: (c: string) => void; onImageUrlChange: (u: string) => void; onSizeChange: (s: string) => void }
// Feature 566: BackgroundEditor
export default function BackgroundEditor({ type, color, imageUrl, size, repeat, position, onTypeChange, onColorChange, onImageUrlChange, onSizeChange }: Props) {
  return (<div className="p-3 border-b"><div className="text-xs font-semibold text-gray-500 uppercase mb-2">Background</div><div className="flex gap-1 mb-2">{['solid','gradient','image','none'].map(t => <button key={t} onClick={() => onTypeChange(t)} className={'px-2 py-0.5 text-xs rounded border capitalize ' + (type===t?'bg-blue-50 border-blue-300':'')}>{t}</button>)}</div>{type==='solid' && <div className="flex gap-1"><input type="color" value={color} onChange={e => onColorChange(e.target.value)} className="w-6 h-6" /><input value={color} onChange={e => onColorChange(e.target.value)} className="flex-1 text-xs border rounded px-1 font-mono" /></div>}{type==='image' && <><input value={imageUrl} onChange={e => onImageUrlChange(e.target.value)} placeholder="Image URL" className="w-full text-xs border rounded px-2 py-1 mb-1" /><select value={size} onChange={e => onSizeChange(e.target.value)} className="w-full text-xs border rounded px-2 py-1"><option>cover</option><option>contain</option><option>auto</option></select></>}</div>)
}