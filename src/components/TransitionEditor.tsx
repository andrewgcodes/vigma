'use client'
interface Props { property: string; duration: number; easing: string; delay: number; onPropertyChange: (p: string) => void; onDurationChange: (d: number) => void; onEasingChange: (e: string) => void; onDelayChange: (d: number) => void }
// Feature 557: TransitionEditor
export default function TransitionEditor({ property, duration, easing, delay, onPropertyChange, onDurationChange, onEasingChange, onDelayChange }: Props) {
  return (<div className="p-3 border-b"><div className="text-xs font-semibold text-gray-500 uppercase mb-2">Transition</div><div className="grid grid-cols-2 gap-2 text-xs"><div><label className="text-gray-400">Property</label><select value={property} onChange={e => onPropertyChange(e.target.value)} className="w-full border rounded px-1 py-0.5"><option>all</option><option>opacity</option><option>transform</option><option>background</option></select></div><div><label className="text-gray-400">Duration (ms)</label><input type="number" value={duration} onChange={e => onDurationChange(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div><div><label className="text-gray-400">Easing</label><select value={easing} onChange={e => onEasingChange(e.target.value)} className="w-full border rounded px-1 py-0.5"><option>ease</option><option>linear</option><option>ease-in</option><option>ease-out</option></select></div><div><label className="text-gray-400">Delay (ms)</label><input type="number" value={delay} onChange={e => onDelayChange(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div></div></div>)
}