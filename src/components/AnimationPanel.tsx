'use client'
interface Props { type: string; duration: number; easing: string; delay: number; onTypeChange: (t: string) => void; onDurationChange: (d: number) => void; onEasingChange: (e: string) => void; onDelayChange: (d: number) => void }
// Feature 492: AnimationPanel
export default function AnimationPanel({ type, duration, easing, delay, onTypeChange, onDurationChange, onEasingChange, onDelayChange }: Props) {
  return (<div className="p-3 border-b"><div className="text-xs font-semibold text-gray-500 uppercase mb-2">Animation</div><div className="mb-2"><select value={type} onChange={e => onTypeChange(e.target.value)} className="w-full text-xs border rounded px-2 py-1"><option value="none">None</option><option value="fade">Fade</option><option value="slide">Slide</option><option value="scale">Scale</option><option value="rotate">Rotate</option></select></div><div className="grid grid-cols-2 gap-2 text-xs"><div><label className="text-gray-400">Duration</label><input type="number" value={duration} onChange={e => onDurationChange(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div><div><label className="text-gray-400">Delay</label><input type="number" value={delay} onChange={e => onDelayChange(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div></div><div className="mt-2"><select value={easing} onChange={e => onEasingChange(e.target.value)} className="w-full text-xs border rounded px-2 py-1"><option value="linear">Linear</option><option value="ease">Ease</option><option value="ease-in">Ease In</option><option value="ease-out">Ease Out</option><option value="ease-in-out">Ease In Out</option></select></div></div>)
}