'use client'
interface Props { format: string; scale: number; quality: number; previewUrl: string; onFormatChange: (f: string) => void; onScaleChange: (s: number) => void; onQualityChange: (q: number) => void }
// Feature 499: ExportPreviewPanel
export default function ExportPreviewPanel({ format, scale, quality, previewUrl, onFormatChange, onScaleChange, onQualityChange }: Props) {
  return (<div className="p-3 border-b"><div className="text-xs font-semibold text-gray-500 uppercase mb-2">Export Preview</div>{previewUrl && <div className="w-full h-32 bg-gray-100 rounded mb-2 bg-center bg-contain bg-no-repeat" style={{backgroundImage:'url('+previewUrl+')'}} />}<div className="grid grid-cols-3 gap-2 text-xs"><div><label className="text-gray-400">Format</label><select value={format} onChange={e => onFormatChange(e.target.value)} className="w-full border rounded px-1 py-0.5"><option>PNG</option><option>SVG</option><option>JPG</option><option>WebP</option></select></div><div><label className="text-gray-400">Scale</label><select value={scale} onChange={e => onScaleChange(Number(e.target.value))} className="w-full border rounded px-1 py-0.5"><option value={1}>1x</option><option value={2}>2x</option><option value={3}>3x</option></select></div><div><label className="text-gray-400">Quality</label><input type="range" min={1} max={100} value={quality} onChange={e => onQualityChange(Number(e.target.value))} className="w-full" /></div></div></div>)
}