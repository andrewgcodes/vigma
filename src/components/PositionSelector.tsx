'use client'
interface Props { position: string; onChange: (p: string) => void; top?: number; right?: number; bottom?: number; left?: number; onTopChange?: (v: number) => void; onRightChange?: (v: number) => void; onBottomChange?: (v: number) => void; onLeftChange?: (v: number) => void }
// Feature 560: PositionSelector
export default function PositionSelector({ position, onChange, top, right, bottom, left, onTopChange, onRightChange, onBottomChange, onLeftChange }: Props) {
  return (<div className="p-3 border-b"><div className="text-xs font-semibold text-gray-500 uppercase mb-2">Position</div><select value={position} onChange={e => onChange(e.target.value)} className="w-full text-xs border rounded px-2 py-1 mb-2"><option>static</option><option>relative</option><option>absolute</option><option>fixed</option><option>sticky</option></select>{position!=='static' && <div className="grid grid-cols-2 gap-2 text-xs"><div><label className="text-gray-400">Top</label><input type="number" value={top||0} onChange={e => onTopChange?.(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div><div><label className="text-gray-400">Right</label><input type="number" value={right||0} onChange={e => onRightChange?.(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div><div><label className="text-gray-400">Bottom</label><input type="number" value={bottom||0} onChange={e => onBottomChange?.(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div><div><label className="text-gray-400">Left</label><input type="number" value={left||0} onChange={e => onLeftChange?.(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div></div>}</div>)
}