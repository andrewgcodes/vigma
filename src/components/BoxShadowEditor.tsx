'use client'
interface Props { shadows: Array<{x:number;y:number;blur:number;spread:number;color:string;inset:boolean}>; onAdd: () => void; onRemove: (index: number) => void; onChange: (index: number, shadow: {x:number;y:number;blur:number;spread:number;color:string;inset:boolean}) => void }
// Feature 567: BoxShadowEditor
export default function BoxShadowEditor({ shadows, onAdd, onRemove, onChange }: Props) {
  return (<div className="p-3 border-b"><div className="flex justify-between items-center mb-2"><span className="text-xs font-semibold text-gray-500 uppercase">Box Shadow</span><button onClick={onAdd} className="text-xs text-blue-500">+ Add</button></div>{shadows.map((s,i) => <div key={i} className="border rounded p-2 mb-1"><div className="grid grid-cols-4 gap-1 text-xs mb-1">{[{l:'X',v:s.x,k:'x'},{l:'Y',v:s.y,k:'y'},{l:'Blur',v:s.blur,k:'blur'},{l:'Spread',v:s.spread,k:'spread'}].map(f => <div key={f.k}><label className="text-gray-400 text-[10px]">{f.l}</label><input type="number" value={f.v} onChange={e => onChange(i,{...s,[f.k]:Number(e.target.value)})} className="w-full px-1 py-0.5 border rounded" /></div>)}</div><div className="flex items-center gap-2"><input type="color" value={s.color} onChange={e => onChange(i,{...s,color:e.target.value})} className="w-5 h-5" /><label className="text-xs flex items-center gap-1"><input type="checkbox" checked={s.inset} onChange={e => onChange(i,{...s,inset:e.target.checked})} />Inset</label><button onClick={() => onRemove(i)} className="text-xs text-red-400 ml-auto">&times;</button></div></div>)}</div>)
}