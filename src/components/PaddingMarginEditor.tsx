'use client'
interface Props { paddingTop: number; paddingRight: number; paddingBottom: number; paddingLeft: number; marginTop: number; marginRight: number; marginBottom: number; marginLeft: number; onPaddingChange: (side: string, val: number) => void; onMarginChange: (side: string, val: number) => void }
// Feature 565: PaddingMarginEditor
export default function PaddingMarginEditor({ paddingTop, paddingRight, paddingBottom, paddingLeft, marginTop, marginRight, marginBottom, marginLeft, onPaddingChange, onMarginChange }: Props) {
  return (<div className="p-3 border-b"><div className="text-xs font-semibold text-gray-500 uppercase mb-2">Spacing</div><div className="mb-2"><div className="text-xs text-gray-400 mb-1">Padding</div><div className="grid grid-cols-4 gap-1">{[{s:'top',v:paddingTop},{s:'right',v:paddingRight},{s:'bottom',v:paddingBottom},{s:'left',v:paddingLeft}].map(p => <input key={p.s} type="number" value={p.v} onChange={e => onPaddingChange(p.s,Number(e.target.value))} className="text-xs border rounded px-1 py-0.5 text-center" placeholder={p.s[0].toUpperCase()} />)}</div></div><div><div className="text-xs text-gray-400 mb-1">Margin</div><div className="grid grid-cols-4 gap-1">{[{s:'top',v:marginTop},{s:'right',v:marginRight},{s:'bottom',v:marginBottom},{s:'left',v:marginLeft}].map(m => <input key={m.s} type="number" value={m.v} onChange={e => onMarginChange(m.s,Number(e.target.value))} className="text-xs border rounded px-1 py-0.5 text-center" placeholder={m.s[0].toUpperCase()} />)}</div></div></div>)
}