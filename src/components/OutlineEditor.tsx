'use client'
interface Props { width: number; style: string; color: string; offset: number; onWidthChange: (w: number) => void; onStyleChange: (s: string) => void; onColorChange: (c: string) => void; onOffsetChange: (o: number) => void }
// Feature 577: OutlineEditor
export default function OutlineEditor({ width, style, color, offset, onWidthChange, onStyleChange, onColorChange, onOffsetChange }: Props) {
  return (<div className="p-3 border-b"><div className="text-xs font-semibold text-gray-500 uppercase mb-2">Outline</div><div className="grid grid-cols-2 gap-2 text-xs"><div><label className="text-gray-400">Width</label><input type="number" value={width} onChange={e => onWidthChange(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div><div><label className="text-gray-400">Style</label><select value={style} onChange={e => onStyleChange(e.target.value)} className="w-full border rounded px-1 py-0.5"><option>solid</option><option>dashed</option><option>dotted</option><option>double</option></select></div><div><label className="text-gray-400">Color</label><input type="color" value={color} onChange={e => onColorChange(e.target.value)} className="w-full h-6" /></div><div><label className="text-gray-400">Offset</label><input type="number" value={offset} onChange={e => onOffsetChange(Number(e.target.value))} className="w-full px-1 py-0.5 border rounded" /></div></div></div>)
}